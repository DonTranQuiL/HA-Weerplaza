[pytest]
asyncio_mode = auto

# safer default (avoids plugin warnings in CI)
asyncio_default_fixture_loop_scope = function

# HARD disable socket blocking plugins
addopts =
    -p no:socket
    -p no:pytest_socket
